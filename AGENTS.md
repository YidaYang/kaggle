# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a minimal/empty project containing only a `README.md`. There are no application services, dependencies, build systems, linters, test frameworks, or source code to run.

- **No dependencies** to install (no `package.json`, `requirements.txt`, `go.mod`, etc.).
- **No services** to start.
- **No tests** or lint checks configured.
- **No build step** required.

When the repository gains actual application code, this file should be updated with relevant development environment instructions.
